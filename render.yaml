services:
  - type: web
    name: tubelite-cpp
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 10000
      - key: DATA_DIR
        value: /app/data
